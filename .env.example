# Обязательная переменная — токен от @BotFather
BOT_TOKEN=

# Один из вариантов публичного HTTPS-адреса:
# На Railway можно не задавать WEBHOOK_URL, если сервису создан Public Domain:
# тогда автоматически используется RAILWAY_PUBLIC_DOMAIN.
WEBHOOK_URL=https://your-bot.up.railway.app

# Постоянный секрет: 8–256 символов A-Z, a-z, 0-9, _ или - (без пробелов/кавычек).
# Сгенерировать: python -c "import secrets; print(secrets.token_urlsafe(32))"
WEBHOOK_SECRET_TOKEN=

ADMIN_IDS=
DB_PATH=/data/iskra.db
DB_POOL_SIZE=8

# Опционально
REDIS_URL=
